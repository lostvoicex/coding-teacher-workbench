# 编程老师工作台 - 部署到 GitHub Pages 指南（永久在线·不休眠）

> GitHub Pages 是 GitHub 提供的免费静态网站托管，**永久在线、24 小时不休眠、免费**。
> 你的工作台是单文件应用，只需上传 **1 个文件**即可完成部署。

---

## 准备工作

- 需要：一个 GitHub 账号（没有的话先到 https://github.com/signup 注册）
- 需要上传的文件：`github-pages-upload/index.html`（已为你准备好，就是这个文件夹里的 `index.html`）

---

## 第一步：创建仓库

1. 浏览器打开 GitHub 并登录：https://github.com/login
2. 点击右上角 **「+」** 号 → 选择 **「New repository」**（新建仓库）
3. 填写仓库信息：
   - **Repository name**（仓库名）：输入 `coding-teacher-workspace`
   - **Public**（公开）：保持选中（免费 Pages 需要公开仓库）
   - **不要勾选** Add a README file（保持空白即可）
4. 点击绿色按钮 **「Create repository」** 创建仓库

## 第二步：上传工作台文件

1. 进入刚创建的仓库页面，点击 **「uploading an existing file」**（上传现有文件）链接
   （或者点页面上的 **「Add file」** → **「Upload files」**）
2. 把本机文件夹 `github-pages-upload` 里的 **`index.html`** 直接**拖拽**到网页中间的上传区域
3. 拖动到上传区后，点击绿色按钮 **「Commit changes」**（提交）
4. 等待几秒，文件上传完成

## 第三步：开启 GitHub Pages

1. 在仓库页面顶部点击 **「Settings」**（设置）
2. 左侧菜单点击 **「Pages」**（页面，在左侧下方）
3. 找到 **「Build and deployment」**（构建和部署）区域：
   - **Source**（来源）选择：`Deploy from a branch`（从分支部署）
   - **Branch**（分支）选择：`main`，右侧目录选择 `/ (root)`（根目录）
4. 点击 **「Save」**（保存）

## 第四步：获取你的永久链接

1. 保存后页面会显示提示：**"Your site is live at ..."**（你的站点已上线）
2. 链接格式为：`https://你的用户名.github.io/coding-teacher-workspace/`
   - 例如用户名叫 `zhangsan`，链接就是 `https://zhangsan.github.io/coding-teacher-workspace/`
3. **第一次部署可能需要 1~5 分钟生效**，稍等后用手机/电脑浏览器打开即可
4. 建议把链接保存到手机桌面（参考第 6 节），以后随时打开，永不休眠

---

## 第五步（重要）：迁移你的教学数据

> ⚠️ GitHub 链接和之前的云端链接是**不同的网址**，浏览器会把数据分开存放。
> 你需要把旧链接里的数据**导出 → 导入**到新链接，一次搞定，之后数据就跟着新链接走了。

### 导出（在旧链接操作）
1. 用手机/电脑打开旧链接：https://ca15aa2ccd46425f92bc2311d0665d4b.app.workbuddy.link
2. 底部导航点 **「更多」** → 进入 **「数据管理」**
3. 点 **「导出备份」**（导出 JSON 文件），保存到手机/电脑

### 导入（在新链接操作）
1. 打开新链接：`https://你的用户名.github.io/coding-teacher-workspace/`
2. 底部导航点 **「更多」** → 进入 **「数据管理」**
3. 点 **「导入备份」** → 选择刚才导出的 JSON 文件
4. 提示成功后，学员、作业、报告、设置等所有数据全部迁移完成 ✅

---

## 以后怎么更新工作台（换新版本）

以后如果工作台功能更新，只需要：
1. 拿到最新的 `index.html`
2. 打开 GitHub 仓库页面 → 点 **「Add file」** → **「Upload files」**
3. 拖入新文件（会提示覆盖），点 **「Commit changes」**
4. 等 1~2 分钟，新版本自动生效，**链接不变**

---

## 手机添加到桌面（像 App 一样用）

### iPhone (Safari)
1. Safari 打开你的新链接 → 点底部 **「分享」**（方框+箭头图标）
2. 选择 **「添加到主屏幕」** → 点 **「添加」**

### Android (Chrome)
1. Chrome 打开你的新链接 → 点右上角 **「⋮」三个点**
2. 选择 **「添加到主屏幕」** → 点 **「添加」**

---

## 常见问题

**Q：提示 404 怎么办？**
A：刚开启 Pages 还在构建，等 1~5 分钟再刷新。若一直 404，检查仓库名和链接拼写（注意大小写），或确认 Settings → Pages 里 Branch 已选 `main` 和 `/ (root)`。

**Q：浏览器打开是空白页？**
A：可能是网络波动，刷新一次；若仍空白，说明文件未上传成功，回仓库确认 `index.html` 在根目录。

**Q：想用自定义域名（如 www.自己的域名.com）？**
A：GitHub Pages 设置里可以绑定自己的域名（需额外配置 DNS），需要时再说。
